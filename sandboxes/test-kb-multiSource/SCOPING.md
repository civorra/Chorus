# Corpus scoping — test-kb-multiSource

## Corpus reviewed
| File | Pages / size | Notes |
|---|---|---|
| corpus/001-cir-binding.md | ~1p | CIR-1.0 — binding regulation, load + crypto requirements |
| corpus/002-arf-toolbox.md | ~1p | ARF-2.1 — technical toolbox, algorithm table + guidance |

## Agents (proposed pipeline order)
| # | Slug | Intent | Consumes (slots) | Produces (slots) |
|---|---|---|---|---|
| 1 | agent-load | Load conformance — CIR Art.5 — P1/P2/P3 minimum load thresholds in Newtons | — | load_ok, motif_load |
| 2 | agent-algo | Cryptographic algorithm compliance — CIR Art.6 / ARF §3.2 — approved list + minimum key length | — | algo_ok, motif_algo |

## Domain Frames
| Frame | Key slots | Notes |
|---|---|---|
| widget | type_element, perf_class, load_n, algorithm, key_length_bits | persistent test element |

## Inter-Frame relationships
(none for this test sandbox)

## Control slots
| Frame.slot | Control slot | Corpus justification |
|---|---|---|
| widget.load_n | _NEEDED | CIR Art.5 — "minimum load" implies derivation possible from perf_class if absent |

## BOARD slots
(none for this test sandbox)

## Pre-checks performed
- [x] Rotated-header matrix table scan — none found (ARF §3.2 table has borders)
- [x] Grammar/identifier alternate-form search — perf_class: P1/P2/P3 only, no alternate form documented

## Corpus section assignment
Generated by chorus-corpus-scoping Phase 2 — test

| Section corpus | Agent primaire | Agents contexte | Statut |
|---|---|---|---|
| CIR — Foreword | — | — | OUT-OF-SCOPE |
| CIR — Terms and definitions | — | agent-load, agent-algo | CONTEXT-ONLY |
| CIR — Article 5 — Load conformance | agent-load | — | PRIMARY |
| CIR — Article 6 — Cryptographic parameters | agent-algo | — | PRIMARY |
| CIR — Annex A (informative) | — | — | OUT-OF-SCOPE |
| CIR — Annex C (informative) — empirical benchmark data | — | agent-algo | HELPER-SOURCE |
| CIR — Annex D (informative) — load-test methodology | — | agent-load | CONTEXT-ONLY |
| ARF — Scope | — | — | OUT-OF-SCOPE |
| ARF — §3.1 — Performance class definitions | — | agent-load, agent-algo | CONTEXT-ONLY |
| ARF — §3.2 — Approved algorithm list | agent-algo | agent-load | PRIMARY |
| ARF — §4.1 — Implementation guidance | — | agent-algo | CONTEXT-ONLY ⚠️ override CORPUS-DIRECTIVES |
| ARF — Annex B (informative) | — | — | OUT-OF-SCOPE |

> **Résultat d'exécution réelle de la Phase 2 (2026-09-20)** — voir
> `TEST-PLAN-helper-source.md` pour le protocole et le détail du calcul.
>
> - **Annex C** → `HELPER-SOURCE`, déterminé par `STEP 0b` (correctif) :
>   renvoi structurel depuis Art.6 (PRIMARY, seuil numérique) **et**
>   `[MATRIX TABLE]` présente dans la section → les deux conditions sont
>   réunies, `STEP 0b` court-circuite le scoring PRIMARY avant qu'il ne
>   s'exécute. **Test réussi** : sans le correctif d'ordonnancement
>   `STEP 0b`, cette section aurait été verrouillée `PRIMARY` sur
>   `agent-algo` par recouvrement de vocabulaire (`minimum`, `key`, `length`,
>   `ARF` — score = 4), empêchant le raffinement `HELPER-SOURCE` de
>   s'appliquer.
> - **Annex D** → `CONTEXT-ONLY`, confirmé par `STEP 0c` (2ᵉ correctif,
>   2026-09-20) : la section ne contient aucune clause déontique
>   (`shall`/`must`) propre — uniquement du descriptif ("are performed",
>   "documents") — `STEP 0c` force `score = 0` pour tous les agents avant
>   même le calcul du recouvrement de vocabulaire, qui aurait sinon donné
>   `score(agent-load, Annex D) = 2` (`load`, `threshold`) et verrouillé la
>   section en `PRIMARY` à tort. Classification désormais obtenue par
>   l'algorithme seul, sans jugement manuel — voir
>   `Engine/WRK-BUG-corpus-scoping-primary-keyword-overmatch.md` (corrigé)
>   et `chorus-corpus-scoping.md` § STEP 0c.

## Open questions for the operator
(none — synthetic corpus, all sections designed explicitly)

## Status
`CONFIRMED — test`
